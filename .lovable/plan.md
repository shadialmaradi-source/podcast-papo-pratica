

# Apply Git Patch

Exact line-level edits to 2 files:

## 1. `src/components/ProfilePage.tsx`

### Edit A — Line 136: Add `created_at` to first select
### Edit B — Line 154: Add `created_at` to second select  
### Edit C — Line 159: Replace `setMyLessons` with sorted merge
### Edit D — Line 658: Add `totalVideosCompleted > 0` guard

## 2. `src/utils/languageUtils.ts`

### Edit E — After line 63: Append `normalizeLanguageCode` function

No new files. No architecture changes. Direct line replacements.

